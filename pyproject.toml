[project]
name = "order-enquiry-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "psycopg2-binary",
    "python-multipart",
    "openpyxl",
    "httpx",
    "fpdf2",
]

[tool.vercel]
entrypoint = "backend.app.main:app"
