# Order Enquiry Frontend

React + Vite single-page app for the Order Enquiry system. See the [root README](../README.md) for full setup instructions.

```
npm install
npm run dev
```

Dev server runs on http://localhost:5173 and proxies `/api` requests to the backend on http://localhost:8000 (see `vite.config.js`).
